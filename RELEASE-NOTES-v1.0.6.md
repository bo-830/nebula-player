# NEBULA Player 1.0.6 — 首个公开版本

> 本文件可直接作为 GitHub Release 说明使用（发布说明按「首版」撰写）。
> 资源：`nebula-player-1.0.6-setup.exe`（约 113 MB）、`.blockmap`、`latest.yml`

## 这是什么

**NEBULA Player** —— 深空黑科技风的**本地**音乐播放器（Windows）。支持
MP3 / WAV / FLAC / AAC / APE / OGG，四维分类与全局搜索，歌单与收藏，系统托盘与进度记忆，
内置歌词与实时频谱；AI 音乐助手 **薇拉 Vela** 通过你自己的 OpenAI 兼容接口工作。

**隐私与离线**：音乐播放全程离线，只有 AI 对话会联网；API Key 经系统安全存储加密（DPAPI）保存。

## 亮点

### 🎛 迷你窗：展开即是「搜索 + 聊天」
- 收起时 **360×128** 的悬浮歌词条；点展开按钮变成 **360×540**：**上方搜索框、下方 AI 聊天框**
- 搜索结果点击即在主窗口播放（以结果列表为队列）；迷你窗里可以直接指挥播放器、也能聊天
- 再次收起完全还原；**每次打开都从收起态开始**；窗口始终置顶、可拖动、不占任务栏
- 展开不会跑出屏幕：按显示器工作区自动裁剪与收边

### 🎙 AI 音乐助手「薇拉 Vela」
- **执行指令**（播放/暂停/切歌/音量/建歌单）：一句话给结论，不寒暄
- **推荐与闲聊**：温和、有见地，可以卖萌、可以用 emoji
- **破坏性操作必须先确认**：会明确写出「从《歌单名》移除 N 首：A、B、C」，你点确认才执行
- 找不到的歌会直说没有，并给出最接近的 3 首；不会承诺做不到的事
- 主窗口与迷你窗显示同一个对话，历史本地持久化

### ✨ 本次一并带来的改进
- 🎵 **实时频谱**：修复了频谱此前恒为零的问题（自定义协议权限 + 跨域响应头）
- ⏱ **睡眠定时器**：15/30/60 分钟，或「播完当前歌曲 / 播完当前队列」自动停
- 🎚 **歌词偏移校准**：±0.5 秒微调，**每首歌各记各的**，详情页与迷你窗同步
- ⏭ **列表循环**：修复播放到最后一首不再回到第一首的问题
- 📂 **扫描即可播**：新添加的文件夹立即能播，不再需要重启
- 🔐 **安全收紧**：本地媒体协议只允许白名单扩展名与指定目录内的文件
- 🛠 **稳定性与质量**：升级路径不再因旧配置缺字段而报错；AAC/APE 转码修复；ESLint 归零；单元测试 179 项
- 📦 **安装包瘦身**：程序主体从 186.7 MB 降到 **25.1 MB**

## 安装

1. 下载 `nebula-player-1.0.6-setup.exe`
2. 双击安装（Windows 会提示未知发布者 —— 当前版本未做代码签名，选择「更多信息 → 仍要运行」即可）
3. 首次使用：点「添加音乐」选择文件夹 → 双击歌曲播放
4. 想用 AI：设置 → AI 配置，填入接口地址（如 `https://api.deepseek.com/v1`）、API Key、模型名，点「测试连接」

**校验**：安装包的 SHA-512 见 `latest.yml`（可自行复算比对）。

## 系统要求

- Windows 10/11 64 位
- 无需安装额外运行库（ffmpeg 已内置）

## 已知限制（诚实清单）

- 安装包未做代码签名，首次安装会出现 SmartScreen 提示
- APE 格式仅做了分支级验证（本机无 APE 编码器，未做端到端实测）
- 曲库按 ≤2 万首设计（JSON 持久化 + 内存索引），更大规模建议等待后续 SQLite 版本
- 系统媒体控件（Windows 媒体弹窗）的封面暂不显示（应用内封面正常）
- 极小概率下，扫描文件夹可能报告失败；**重新扫描即可**（已列入下版修复）
- AI 的"口吻分档"由提示词约束，极端情况下模型可能多带一个表情；自动截断将在下版加入
