# CAPTAIN NOTE — 1.0.5 为内部版本，1.0.6 为对外第一版

- 记录人：captain
- 用户裁定时间：本次会话（用户回复「没有」= 1.0.5 未对外分发）

## 事实

| 版本 | 构建/安装 | 内容 | 对外状态 |
|---|---|---|---|
| 1.0.4 | 已构建、已 `/S` 覆盖安装 | 六项改进 + r3 修复波（R1 BLOCKER / R2 / R3 / R4） | 已交付（本机在装） |
| **1.0.5** | **已构建、已 `/S` 覆盖安装**（`dist/nebula-player-1.0.5-setup.exe` 118,123,909 B @12:55:05Z；安装位 `ProductVersion 1.0.5.0`；asar 26,331,161 B / `6E165067…`） | t50 的 **F1 修复**（hidden 托盘 + 迷你窗代理丢回复） + t54 的**薇拉 Vela** 人格与双窗标题 | **内部版本，未对外分发**（用户确认） |
| 1.0.6（计划） | 待构建 | 1.0.5 全部内容 + **H1 修复**（打字机追尾导致 AI 长回答被截断并落库） | **对外第一版** |

## 为什么 1.0.5 不作为对外版本

`1.0.5` 的构建与安装发生在 **H1 被确认之前**（事故/时序：ui-features 按 `t53` 契约执行，而 captain 的暂缓令只发给了 verifier、未广播 —— 责任记为 captain）。H1 是**用户可见**缺陷：真实模型滴注 30–100 字符/秒 vs 打字机揭示 227 字符/秒 ⇒ 游标追尾后 chunk 守卫丢弃后续所有 delta ⇒ **回答被截断并按截断内容落库**；夹具级（reviewer §13）与运行期（verifier t57，2000ms 档）双双复现。

## 处置

1. 1.0.5 的安装包**不要对外分发**（用户已确认未对外分发）。**用户已批准**：在 **1.0.6 构建并安装成功之后**，把 `dist/nebula-player-1.0.5-setup.exe` 与 `.blockmap` 移入 **`dist/internal/`**，避免误发；该动作作为 **`t61`（1.0.6 发布）** 的收尾项执行（1.0.6 的 `latest.yml` 会指向 1.0.6，故移动 1.0.5 不会破坏更新 feed）。
2. 1.0.6 的发布说明按"**首版**"撰写；变更内容照常列出 F1/H1 两项修复与薇拉 Vela 人格，但不必写成"修复已发布缺陷"。
3. 版本号保持单调递增：**不为 1.0.5 打补丁、不重发 1.0.5**。

## 关联任务

- `t58` H1 修复（ai-tools）→ `t59` 复验（verifier）→ `t60` 审查（reviewer）
- `t55` 薇拉 Vela 人格验证（quality，自 verifier 改派）→ `t56` 审查（reviewer）
- **`t61` 1.0.6 发布**（verifier，deps = t56 + t60）：额外硬性断言 = **asar 内不得含 H1 旧模式** + **打包态慢滴注不截断**（本地 mock、两段间隔 ≥1000ms）
