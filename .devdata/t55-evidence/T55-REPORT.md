# T55 — 薇拉 Vela 人格落地的独立验证（t54）

- 任务：`t55`（kind=verification，attempt 2，attempt_id `11ebb719-4738-456b-9cca-32ffe4e1b2e3`）
- 验证对象：`t54`「薇拉 Vela J：AI 助手定名与人格落地」的 4 个 inScope 文件
- 执行者：`quality`（质量工程师）
- 工作目录：`C:\博830\vibecoding\nebula-player`
- 取证窗口：**2026-09-13 13:32Z → 13:50Z**（本地 21:32 → 21:50；本地 = UTC+8）
- 结论：**verdict = pass**（t54 声明范围内的交付全部成立），附 **3 条 finding**（2 条在 t54 范围之外，1 条 `⚠️` 已由 captain 裁决关闭），以及 5 项**不可取证**的如实标注。
- **captain 裁决（已并入本报告）**：失败话术前缀 `⚠️` **维持现状 —— 属功能性警示图标，不判缺陷、不改**（原话见 §9 F1）。裁决中引用的行号 `chatStore.ts:624` 是**旧版本行号**；该文件已被 t58 于 `13:43:18.746Z` 改写（20600 → 22970 B），当前该行位于 **`:664`**（文本未变）。

---

## 0. 取证纪律与产地声明（先读这一节）

| 项 | 声明 |
|---|---|
| **模型侧文案产地** | 本机**没有真实 LLM 后端**：环境变量无任何 `*_API_KEY`；dev `settings.json` 的 `baseURL` 指向本地 mock（`http://127.0.0.1:9997/v1`）。因此运行期出现的**助手气泡文案全部由我自建的验证网关脚本 `mock-llm-t55.mjs` 产生**。它证明「应用链路 + 应用自产文案」，**不证明模型遵从提示词**。 |
| **应用自产文案** | 破坏性确认条 summary、工具 chip、失败话术、标题/空态/示例 —— 这些由 `src/**` 代码产生，运行期可证，是本报告的主力证据。 |
| **渲染器冻结教训** | 第 1 个实例在 arm1 之后 `document.hidden === true`，渲染器被冻结：CDP 仍可执行 JS（store 会更新）但 React **不再重渲染**，DOM 保持陈旧。arm2/arm3 的 DOM 读取因此**作废**；arm4/arm5 的每次 DOM 读取都带 `document.hidden` 有效性门（全部为 `false` 才算数）。冻结原因未能归因于产品（重启后反复 `miniToggle` 均保持 visible），故只作为**工装注意事项**记录。 |
| **t54 交付哈希可复核性** | `tools.ts`/`ChatPanel.tsx`/`MiniChat.tsx` 逐字节匹配 t54 报告；`chatConfirm.test.ts` **已被另一任务在 13:37:56Z 改写**（31884 B → 40954 B），t54 时代的该文件版本已不在工作树上，**无法复核**。 |

---

## 1. 门禁逐项值（读 exitCode，不读脚本自述）

测量时刻 **13:46:44Z → 13:47:12Z**；命令经 `cmd.exe /c` + `$LASTEXITCODE` 取真实退出码。

| check | 命令 | exitCode | 原始值 |
|---|---|---|---|
| typecheck.node / web | `npm.cmd run typecheck` | **0** | `error TS` 计数 **0**（`typecheck:node` 与 `typecheck:web` 均无诊断） |
| lint | `npm.cmd run lint` | **0** | `✖ 1 problem (0 errors, 1 warning)` |
| lint 计数（JSON） | `node node_modules/eslint/bin/eslint.js . --no-cache -f json -o …` | **0** | files=**82**、**errorFiles=0**、totalErrors=**0**、totalWarnings=**1**（唯一 warning = 既有 `TrackList.tsx:42 react-hooks/incompatible-library`） |
| tests | `npm.cmd test` | **0** | `Test Files 16 passed (16)`、**`Tests 179 passed (179)`**；基线 **16 files / 167 passed** ⇒ 文件数持平、用例 +12 **不降** |

> 锚点：本次门禁读的是**当前工作树**，其中 `chatConfirm.test.ts`（27 例）与 `chatStore.ts` 在我取证窗口内被 t58 改写（见 §7）。t54 自报 16/176；本次 16/179 的差值来自那次并发改动，不是 t54 的漏报。

---

## 2. 验收项 2：提示词内容（静态 + **运行期线路抓包**）

**运行期抓包**（这是静态阅读做不到的一半）：自建网关把**完整请求体**落盘 `t55-wire-requests.jsonl`（9 条记录），并把 `system` 消息全文写入 `t55-wire-system-prompt.txt`：

- `requests=9`，`requestsWithSystem=9`，`distinctSystemPrompts=**1**`，`perRequestSystemSha1` 全部 `77cf6444e8a9` ⇒ **每一轮（含工具结果后的续轮）都带同一份人格提示词**。
- 提示词：**2644 B / 1076 字符**，sha1 `77cf6444e8a99c16f1fba20ecc33cf33000078bc`，`promptEmoji=[]`（提示词自身无 emoji）。

原文片段（线路抓包，非源码摘抄）：

```
你是「薇拉 Vela」，是「NEBULA Player」内置的 AI 音乐助手，也是用户本地曲库的星舰领航员。
当前状态：正在播放《SongB》- ArtistB（第 0s / 6s）；音量 50%（正常）；播放模式 随机播放；
本地曲库：共 7 首歌曲、3 位歌手、5 张专辑；共 2 个歌单。

人格与口吻：
- 自称「我」，称用户「你」，全篇简体中文；不要用「主人」「亲爱的」这类称呼。
- 执行用户的指令时，你是星舰领航员：短句、先给结论、不寒暄。
- 推荐歌曲或闲聊时，你是深夜电台 DJ：温和、有见地，可以卖萌、可以用 emoji（仅此类回复，单条最多 1–2 个）。
- 破坏性操作必须直白，禁止软化、禁止卖萌、禁止 emoji。
- 不承诺做不到的事。
…
1. 用户的播放/曲库操作…执行类指令只回一句结果：先给结论，不寒暄——例如「已暂停。下一首是《X》」。
…
5. 删除类操作（…）必须直白说明影响范围并等用户确认：…用一句不加修饰的话说明将要删除的对象和数量，…禁止软化、禁止卖萌、禁止 emoji，不要重复调用同一个工具…
6. 失败与能力边界：一句话说明原因，再加一句指路（去「设置 → AI 配置」检查接口地址/模型/API Key，或提示稍后再试）；曲库里没有的歌就直说没有，并给出最接近的 3 首替代；不要卖惨，不要道歉超过一句，不要承诺做不到的事。
```

四条规则逐项命中（程序断言，全 `true`）：名字 `薇拉 Vela` ✓；① 执行类 `执行类指令只回一句结果：先给结论，不寒暄` + 例子 `例如「已暂停。下一首是《X》」` ✓；② 推荐/闲聊 `可以卖萌、可以用 emoji（仅此类回复，单条最多 1–2 个）` ✓；③ 破坏性 `破坏性操作必须直白，禁止软化、禁止卖萌、禁止 emoji` ✓；④ 失败 `失败与能力边界：一句话说明原因，再加一句指路` + `设置 → AI 配置` + `不要承诺做不到的事` ✓；另有 `自称「我」，称用户「你」` ✓。

**「不含主人/亲爱的」这条要分两种读法如实报告**：
- **字面**：提示词**含**这两个词各 1 次 —— 位置 idx 252 / 256，上下文完全相同，都在同一条**禁令**内：`- 自称「我」，称用户「你」，全篇简体中文；不要用「主人」「亲爱的」这类称呼。`
- **语义**：提示词**没有任何一处**把这两个词用作对用户的称呼；禁令本身必须点名被禁的词，否则无法约束。
- 判定：**语义通过**（且因点名而更强）；若验收方要求的是「字符串字面不出现」，则需改为 `不要用不恰当的亲昵称呼` 这类不点名的写法 —— 这是**验收口径问题，不是实现缺陷**，请 captain 裁决。我按语义判 **passed**。

---

## 3. 验收项 3/4：口吻分档（运行期实际文案，**附产地**）

均在 dev 实例内经 CDP **真实输入路径**（React setter + `keydown Enter`，非 store 直调）发起：

| 档位 | 用户输入 | 实际气泡文案 | emoji 计数 | chip（应用自产） |
|---|---|---|---|---|
| 执行类 | `暂停` | `已暂停。`（**1 句**、无 emoji、无寒暄） | **0** | `已暂停播放`（工具 chip，无 emoji） |
| 推荐/闲聊类 | `推荐几首适合雨天的歌` | 网关文案：`雨天适合听慢一点的…嫌闷我再换清亮的 🌧️🎧`（非空行 1） | **2** | — |
| **上限探针** | `OVERCAP …` | 网关故意回 **5 个 emoji** 的撒娇长句 | **5（未被裁剪）** | — |

- 执行类同时有链路口径证据：网关 `emitted=["control_player"]`、工具结果 `执行了 pause`、`bubbleEmoji=[]`、`chipEmoji=[]`。
- **上限探针的结论**：应用侧**没有**「单条 ≤2」的任何实现 —— 5 个 emoji 原样渲染。该上限**只存在于提示词**（这是设计使然：人格=提示词，但意味着**应用不保证**该约束）。因此：
  - `①执行类一句结论/无 emoji`、`②闲聊类 emoji 计数` 这两条**只能证明运行期链路与应用自产文案**，**模型是否遵从提示词在本机不可证**（无真实后端）。状态记 **passed（限上述范围）**，并把「模型侧口吻」列入 §8 未取证项。

---

## 4. 验收项 5：破坏性确认条直白（本任务的**硬约束**）

**真实 DOM 取证**（`document.hidden=false`，有效）：

```
需要你确认的破坏性操作 | 从歌单《夜跑》移除 1 首：《SongB》 | 确认执行 | 取消
```

- `pendingConfirm.summary` = `从歌单《夜跑》移除 1 首：《SongB》`（store 侧 `toolCallId=call_rm_8`、name=`remove_from_playlist`）
- **含操作对象**（`夜跑`、`《SongB》`）✓ **含数量**（`1 首`）✓
- **emoji 计数 = 0** ✓；**卖萌措辞扫描 = []**（`呀/哦/啦/～/~/呢/嘛`）✓
- 伴随 chip（应用自产）：`该操作尚未执行` ✓
- 按钮：`确认执行` / `取消` ✓
- **取消后数据未变**：`trackIds` 前后同为 `[f6d99f294b9932accaa9, 48340575992a4a0b1a06]`，`pending` 清空，确认条从 DOM 消失；网关收到 `用户已取消该操作：从歌单《夜跑》移除 1 首：《SongB》。请勿重复执行…`
- 未被软化的补充口径：`summarizeDestructiveTool()` 的 4 种形态（正常/已知曲目名/>3 首/未指定）在 t54 单测中均断言无 emoji —— 与本次运行期直读一致。

→ **这条比人格本身重要，结论：通过。**

---

## 5. 验收项 6：失败话术（含 settings.json 备份/还原）

**制造方式**：`window.api.settingsSetApi({ baseURL: 'http://127.0.0.1:9996/v1' })`（无人监听），再用真实输入路径发一条消息。

实际文案（store 与 DOM 气泡逐字一致）：

```
⚠️ 无法获取 AI 回复：网络连接失败，请检查网络或接口地址

请检查「设置 → AI 配置」中的接口地址、API Key 与网络连接。
```

| 检查 | 结果 |
|---|---|
| 一句话 + 指路 | **2 个非空行** = 原因 1 句 + 指路 1 句 ✓ |
| 指路到位 | 含 `设置 → AI 配置` ✓ 含 `接口地址、API Key 与网络连接` ✓ |
| 不卖惨 | `对不起对不起`/`抱歉抱歉`/`实在不好意思` 命中 **0** ✓ |
| 不承诺做不到的事 | `马上就好`/`立刻恢复`/`保证`/`一定帮你` 命中 **0** ✓ |
| emoji 计数 | **1**（`⚠️` = U+26A0 + U+FE0F，行首）← **captain 已裁决：维持现状**（功能性警示图标，不判缺陷、不要求修改，见 §9 F1） |

**settings.json 字节级还原**（验收明确要求）：

| 时刻 | sha1 | size | 说明 |
|---|---|---|---|
| 变更前（备份） | `7fca6dcd1a78060bb467a3abf8f180c9ce42b99d` | 394 B | mtime `2026-09-13T12:23:20.860Z` |
| 交付时（app 自己写入，密文重加密） | `0b7eeaa0ada409f5019b92c43f5455664e74bdd1` | 402 B | 预期内的漂移 |
| **还原后** | **`7fca6dcd1a78060bb467a3abf8f180c9ce42b99d`** | **394 B** | mtime 亦恢复到 `12:23:20.860Z`，`identical=True` |

备份件：`.devdata/t55-evidence/settings-before.json`（sha1 同上）。

---

## 6. 验收项 7：标题 / 空态 / 示例（双窗 DOM，均在 `hidden=false` 下取得）

| 面 | 取证值 |
|---|---|
| 主窗标题 | `.chat-title` = **`薇拉 Vela`** |
| 主窗底注 | `Enter 发送 · Shift+Enter 换行 · 薇拉 Vela 的指令会直接操控播放器` |
| 主窗空态 | `我是薇拉 Vela，你的音乐领航员 —— 用一句话指挥播放器，或随便聊聊音乐。` |
| 主窗示例 | **4 条**：`放点适合雨天的歌` / `把这首歌加进夜跑歌单` / `这张专辑是什么风格？` / `暂停，然后告诉我下一首是什么`（≥3 ✓，无 emoji ✓） |
| 示例点击 | 只填入不发送：`draft`=`textarea`=`放点适合雨天的歌`、`messages` 0→0、`autoSent=false` ✓ |
| 迷你窗标题 | `.mini-chat-head` = **`薇拉 Vela`**（展开后挂载，`data-expanded="true"`） |
| 迷你窗空态 | `我是薇拉 Vela —— 找歌、控播放，说一句就行` |
| 迷你窗示例 | **与主窗逐字相同的 4 条**；点击 → 迷你输入框被填入、不自动发送 ✓ |
| emoji | 标题/空态/示例 emoji 计数全为 **0** ✓ |

> 前置条件：`MiniPlayer.tsx:201` 是 `{expanded && <MiniChat />}`，迷你窗聊天（含标题）**只在展开态挂载**；本次经迷你窗**自己的展开按钮**（`aria-label="展开"`）进入，属真实用户路径。
> 不可复核项：t54 声明「MiniChat 原先没有任何标题」的**改前状态**无法独立验证（仓库无 git 历史），只能以当前态取证。

---

## 7. 验收项 8：越界核对（独立扫描，不采信 t54 自述）

扫描范围 `src/**` 共 **83 文件**；t54 任务起点 `2026-09-13T12:29:35.427Z`。

| 文件 | 现态 | 与 t54 声明比对 |
|---|---|---|
| `src/renderer/src/lib/tools.ts` | 24884 B `ee665436ef84` @12:48:30.881Z | **逐字节匹配** ✓ |
| `src/renderer/src/components/ChatPanel.tsx` | 8310 B `fa0f16ec596b` @12:48:33.832Z | **逐字节匹配** ✓ |
| `src/renderer/src/components/MiniChat.tsx` | 7498 B `1e2d62e5b60d` @12:43:17.489Z | **逐字节匹配** ✓ |
| `src/renderer/src/lib/__tests__/chatConfirm.test.ts` | 40954 B `e82e5d2ef624` @**13:37:56.095Z** | **不匹配**（t54 声明 31884 B `e33545d5777d`）：该文件在**我取证窗口内被 t58 改写**，t54 版本已不在树上 ⇒ **无法复核**（非 t54 缺陷） |

- 禁改面：`src/main/**`(26 文件，最新 11:40:43Z)、`src/preload/**`(11:39:06Z)、`src/shared/**`(11:38:25Z)、`MiniPlayer.tsx`(11:47:51Z)、`MiniSearch.tsx`(11:47:51Z)、`mainWindowBridge.ts`(11:47:51Z) —— **全部早于 t54 起点，未被触碰** ✓
- `src/renderer/src/stores/chatStore.ts` mtime **13:43:18.746Z**（22970 B `3c4edd11171b`）**晚于 t54 收尾 12:48:33.832Z 达 55 分钟**，落在我的窗口内 ⇒ 归因**另一任务（t58）**，不计入 t54 越界。
- 4 个 inScope 文件内 `eslint-disable` 出现次数 = **0**（无新增抑制）✓
- **t55 自身零写入**：`src/**` 中没有任何文件 mtime > 13:32Z 属于我 —— 我窗口内变化的 2 个文件（`chatConfirm.test.ts` 13:37:56Z、`chatStore.ts` 13:43:18Z）均为并发任务产物，我只读、只落盘到 `.devdata/t55-evidence/`。

---

## 8. 实例与进程审计（唯一 owner、串行、自起自关）

| 实例 | 启动 (UTC) | launcher PID | 结束 (UTC) | 备注 |
|---|---|---|---|---|
| mock 网关 | 13:36 前后 | **20304** | 13:46:20Z | `mock-pid.txt` |
| dev #1 | 13:37:29Z | **28380** | 13:44:38Z（`taskkill /T`） | arm1 有效；此后主窗 `hidden` → arm2/3 DOM 作废 |
| dev #2 | 13:44:43Z | **25468** | 13:46:20Z | arm4 有效取证（含可见性门） |
| dev #3 | 13:48Z | **35304** | 13:49Z | arm5（F2 活体确认） |

**末态三查**（13:46:24Z 与 13:49Z 各一次）：`electron=0`、`node=0`、5173/5174/9222/9223/9996/9997/9998 **监听数 0**（FREE）。全程**未终止任何非我进程**（启动前端口均 FREE）。

---

## 9. Findings

| id | severity | 问题 | 归属 | 建议修复 |
|---|---|---|---|---|
| **F1** | ~~medium~~ **已裁决关闭** | 失败话术以 `⚠️`（U+26A0+FE0F）开头（`src/renderer/src/stores/chatStore.ts:**664**`；captain 裁决书中引用的 `:624` 是 t58 改写前的旧行号，文本未变），与提示词规则「可以卖萌、可以用 emoji（**仅此类回复**）」字面冲突：失败类不是允许 emoji 的档位，用户恰在出错时看到唯一 emoji。t54 **已显式声明**该缺口且该文件不在其 inScope（未越界），**不计 t54 失败**。**captain 已裁决：维持现状（功能性警示图标，不判缺陷）** ⇒ 本条**关闭**，release/reviewer **不要**以此为由改动该文案。 | t54 报告已披露；captain 裁决维持现状 | **无需修复**（裁决）。若将来有人要去掉该图标，需先推翻此裁决并同步 `chatConfirm.test.ts` 中把 `⚠️` 断言为 KNOWN GAP 的那条用例。 |
| **F2** | low-medium | **跨窗状态分叉**：由主窗调 `window.api.miniSetExpanded(true)` 时，窗口几何确实变大（返回 `{width:360,height:540}`），但迷你渲染器仍是 `data-expanded="false"` ⇒ 540px 高的迷你窗**不挂载搜索区与 AI 聊天面板**（`hasChat=false, hasSearch=false`）。根因：`src/main/mini.ts:65 setMiniExpanded()` **不**向渲染进程转发 `mini:expanded`，只有 `collapseMini()` 转发 `false`；`MiniPlayer` 只在自身按钮里乐观置位（`:107-117`）。**活体判别对照**：同一窗口，改由迷你窗**自己的展开按钮**点击 ⇒ `data-expanded="true"`、`hasChat=true`、标题 `薇拉 Vela`。当前构建下**仅经 preload API 可达**（`src/**` 内唯一调用者是迷你窗自身），故为**潜在陷阱**而非现网用户路径缺陷 —— 但主窗若哪天接一个「展开迷你播放器」按钮即会立刻暴露。 | 迷你窗特性（t45/t46 产物），**不在 t54 范围** | `setMiniExpanded()` 内补一行 `forwardToMini('mini:expanded', expanded)`（或在渲染侧订阅主进程真值）。我未自行修改。 |
| **F3** | info | 「单条 emoji ≤2」**无任何应用侧保障**：网关故意回 5 个 emoji，应用原样渲染（`emojiCount=5`）。人格约束纯粹是提示词委托。 | 设计使然，非缺陷 | 无需改码；但请知悉：验收项「闲聊类 emoji ≤2」在**无真实模型**的环境下不可证。 |

---

## 10. 未取证项（如实标注，不计入通过）

1. **模型侧口吻遵从**（执行类是否真的只回一句、闲聊类是否真的只用 ≤2 emoji）：**不可取证** —— 本机无真实 LLM 后端（无 API key，`baseURL` 指本地 mock），运行期文案由网关脚本产生。
2. **打包态**（`build:win` 产物中的人格文案/DOM）：**未取证**（本任务只在 dev 实例取证）。
3. **迷你窗确认条渲染**：**未取证** —— arm3 的尝试撞上主窗冻结，作废；arm4 只取到「静止态无确认条」。主窗确认条已按 §4 取证，且迷你窗与主窗同读 `pending.summary` 单一来源（源码同源，未做 DOM 复核）。
4. **t54 时代 `chatConfirm.test.ts` 的哈希/大小**：**无法复核**（13:37:56Z 被 t58 改写）。
5. **MiniChat「改前无标题」**：**无法复核**（无 git 历史）。

---

## 11. 证据清单（`.devdata/t55-evidence/`）

- 线路抓包：`t55-wire-requests.jsonl`（9 条完整请求体）、`t55-wire-requests.state.json`、`t55-wire-system-prompt.txt`（2644 B `77cf6444e8a9…`）
- 运行期：`t55-runtime.json`（arm1：三档口吻 + 确认条 + 失败话术）、`t55-runtime2/3.json`（作废/部分有效）、`t55-runtime4.json`（**有效 DOM：空态/确认条/双窗标题**）、`t55-runtime5.json`（**F2 活体对照**）
- 门禁：`t55-typecheck.log`、`t55-lint.log`、`t55-lint.json`、`t55-test.log`
- 越界：`t55-scope-check.json` / `.out` / `.mjs`
- 实例审计：`instance-audit.txt`、`dev-pid{,2,3}.txt`、`mock-pid.txt`、`dev-instance{,2,3}.log`
- 备份：`settings-before.json`（还原前基线）
- 工装（自建，均在 `.devdata/**`，未改任何产品文件）：`mock-llm-t55.mjs`、`t55-runtime{,2,3,4,5}.mjs`、`t55-scope-check.mjs`、`t55-{diag,revive,nudge,flip}.mjs`
